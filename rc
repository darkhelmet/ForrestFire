safe_gopath_export $PWD
config=`heroku config -s`
export `echo $config | tr ' ' '\n' | grep READABILITY_TOKEN`
export `echo $config | tr ' ' '\n' | grep POSTMARK_TOKEN`
export `echo $config | tr ' ' '\n' | grep FROM`
